import React from 'react';

const Title = () => (
    <div>
        <h1>Weather Finder</h1>
        <h3>Find out the temperatue, weather conditions and more...</h3>
    </div>
);

export default Title;